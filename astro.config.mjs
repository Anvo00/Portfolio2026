// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://anvo00.github.io',
  base: '/Portfolio2026',
  server: {
    host: true,
  },
});
